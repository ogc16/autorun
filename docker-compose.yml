services:
  app:
    build: .
    container_name: autorun-app
    restart: unless-stopped
    environment:
      AUTORUN_JWT_SECRET: local-dev-secret-key-that-is-long-enough-for-hmac-256!!
    ports:
      - "8080:8080"
    volumes:
      - autorun-data:/app/data

volumes:
  autorun-data:
